{
  "testCases": [
    {
      "id": "TC-TRANS-001",
      "summary": "Verify amount filter and sort functionality",
      "preconditions": "User is on the transaction list page with 100 transactions loaded",
      "steps": [
        {
          "step": "Apply amount filter for transactions greater than $1000",
          "expectedResult": "Only transactions with amounts exceeding $1000 are displayed"
        },
        {
          "step": "Sort the filtered results by date in descending order",
          "expectedResult": "Transactions are arranged from newest to oldest date while maintaining the amount filter"
        }
      ],
      "postconditions": "Transaction list shows filtered and sorted results"
    },
    {
      "id": "TC-TRANS-002",
      "summary": "Verify filter clearing on Copilot refresh",
      "preconditions": "User is on the transaction list page with date filter applied for last 30 days",
      "steps": [
        {
          "step": "Trigger Copilot to return new transaction data",
          "expectedResult": "Date filter is automatically cleared"
        },
        {
          "step": "Observe the transaction list",
          "expectedResult": "All new transaction data is displayed without any filters applied"
        }
      ],
      "postconditions": "Transaction list shows unfiltered new data"
    },
    {
      "id": "TC-TRANS-003",
      "summary": "Verify sort persistence with filtered results",
      "preconditions": "User is on the transaction list page with transactions sorted by amount in ascending order",
      "steps": [
        {
          "step": "Apply filter for transaction type 'Credit'",
          "expectedResult": "Only credit transactions are displayed"
        },
        {
          "step": "Verify sort order",
          "expectedResult": "Credit transactions remain sorted by amount in ascending order"
        }
      ],
      "postconditions": "Transaction list shows filtered credit transactions in sorted order"
    },
    {
      "id": "TC-TRANS-004",
      "summary": "Verify multiple column filtering",
      "preconditions": "User is on the transaction list page",
      "steps": [
        {
          "step": "Apply date filter for last 7 days",
          "expectedResult": "Only transactions from the last 7 days are displayed"
        },
        {
          "step": "Apply amount filter for greater than $500",
          "expectedResult": "Only transactions meeting both criteria are displayed: last 7 days AND amount > $500"
        }
      ],
      "postconditions": "Transaction list shows results matching both filter criteria"
    },
    {
      "id": "TC-TRANS-005",
      "summary": "Verify help guide filtering documentation",
      "preconditions": "User has access to the help guide",
      "steps": [
        {
          "step": "Open the help guide",
          "expectedResult": "Help guide is displayed"
        },
        {
          "step": "Navigate to filtering section",
          "expectedResult": "Filtering documentation is visible with examples for each filter type"
        }
      ],
      "postconditions": "User can access comprehensive filtering documentation"
    }
  ]
}